## TODO: После рефакторинга

1. Вернукть сервисы, которые хранят контент для Embed-сообщений 
2. Вернуть приветственное сообщение при входе пользователя на сервер
3. Вернуть авто-выдачу роли при взаимодействие с selection menu
4. Добавить расширенную статистику по участнику (включая новый функционал подсчета времени в голосовых каналах и сообщений в чатах)
5. Добавить команды 
		
- /help — список команд и краткое описание.<br>
- /ping — проверка отклика бота (возвращает «Pong!» и задержку).<br>
- /info — информация о боте (версия, автор, ссылка на репозиторий).<br>
- /uptime — время работы бота с последнего перезапуска.<br>
- /serverinfo — данные о сервере (количество участников, каналов, роль бота).<br>
- /warn @member причина — выдаёт предупреждение (записывает в лог).<br>
- /kick @member — исключает пользователя (с логированием).<br>
- /ban @member — банит пользователя (с возможностью указать срок).<br>
- /mute @member 1h — мутит на указанное время (1h, 30m и т. п.).<br>
- /notes @member — показывает историю предупреждений пользователя.<br>
- /lock — закрывает канал для всех, кроме модераторов.<br>
- /unlock — снимает блокировку канала.<br>

6. Изменить namespace'ы во всех файлах.